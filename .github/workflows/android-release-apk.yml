name: Android Release APK

on:
  push:
    tags:
      - "*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: Existing release tag to build and attach the APK to
        required: true
        type: string
  workflow_call:
    inputs:
      tag_name:
        description: Release tag created by the caller
        required: true
        type: string
    secrets:
      ANDROID_RELEASE_KEYSTORE_BASE64:
        required: true
      ANDROID_RELEASE_STORE_PASSWORD:
        required: true
      ANDROID_RELEASE_KEY_ALIAS:
        required: true
      ANDROID_RELEASE_KEY_PASSWORD:
        required: true

permissions:
  contents: write

concurrency:
  group: android-release-apk-${{ inputs.tag_name || github.ref_name }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build Android release APK
    runs-on: ubuntu-latest
    timeout-minutes: 120

    env:
      ANDROID_NDK_VERSION: 29.0.14206865
      RELEASE_TAG_NAME: ${{ inputs.tag_name || github.ref_name }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.tag_name || github.ref }}
          fetch-depth: 0

      - name: Verify release tag checkout
        run: |
          if ! git show-ref --verify --quiet "refs/tags/${RELEASE_TAG_NAME}"; then
            echo "Release tag does not exist: ${RELEASE_TAG_NAME}" >&2
            exit 1
          fi
          tag_commit="$(git rev-list -n 1 "${RELEASE_TAG_NAME}")"
          head_commit="$(git rev-parse HEAD)"
          if [[ "$tag_commit" != "$head_commit" ]]; then
            echo "Checked out $head_commit, but ${RELEASE_TAG_NAME} points to $tag_commit." >&2
            exit 1
          fi

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: platform-tools

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager --install \
            "platform-tools" \
            "platforms;android-37.0" \
            "build-tools;37.0.0" \
            "ndk;${ANDROID_NDK_VERSION}"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.4"
          channel: stable
          cache: true

      - name: Prepare Flutter workspace
        run: |
          flutter --version
          bash scripts/prepare_flutter_workspace.sh

      - name: Analyze Dart sources
        run: flutter analyze

      - name: Run Dart and widget tests
        run: flutter test

      - name: Restore Android release signing key
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
        run: |
          : "${RELEASE_KEYSTORE_BASE64:?Android release keystore secret is required}"
          keystore_path="${RUNNER_TEMP}/vesper-android-release.p12"
          printf '%s' "$RELEASE_KEYSTORE_BASE64" | base64 --decode > "$keystore_path"
          chmod 600 "$keystore_path"
          echo "VESPER_ANDROID_KEYSTORE_PATH=$keystore_path" >> "$GITHUB_ENV"

      - name: Build release APK
        env:
          VESPER_ANDROID_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
          VESPER_ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          VESPER_ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
        run: flutter build apk --release --target-platform android-arm64

      - name: Remove Android release signing key
        if: always()
        run: rm -f "${RUNNER_TEMP}/vesper-android-release.p12"

      - name: Verify release excludes performance diagnostics
        run: >-
          bash scripts/verify_performance_diagnostics_excluded.sh
          build/app/outputs/flutter-apk/app-release.apk

      - name: Stage APK artifact
        run: |
          mkdir -p artifacts
          tag_name="${RELEASE_TAG_NAME//\//-}"
          artifact_tag="$tag_name"
          if [[ "$tag_name" == *-v[0-9]* ]]; then
            artifact_tag="v${tag_name##*-v}"
          fi
          apk_name="vesper-${artifact_tag}-android-arm64-release.apk"
          apk_path="build/app/outputs/flutter-apk/app-release.apk"
          if [[ ! -f "$apk_path" ]]; then
            apk_path="$(find build/app/outputs/flutter-apk -maxdepth 1 -type f -name '*release*.apk' | sort | head -n 1)"
          fi
          if [[ -z "$apk_path" || ! -f "$apk_path" ]]; then
            echo "Release APK was not found." >&2
            find build/app/outputs -type f -name '*.apk' -print >&2 || true
            exit 1
          fi
          cp "$apk_path" "artifacts/$apk_name"
          (cd artifacts && sha256sum "$apk_name" > "$apk_name.sha256")

      - name: Upload APK artifact
        uses: actions/upload-artifact@v6
        with:
          name: android-release-apk
          path: artifacts/*
          if-no-files-found: error
          retention-days: 30

      - name: Attach APK to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.RELEASE_TAG_NAME }}
          files: artifacts/*
          fail_on_unmatched_files: true
