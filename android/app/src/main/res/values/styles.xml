<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode
    setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="VesperPlayerExternalRouteButtonTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">#FFFFFFFF</item>
        <item name="android:colorBackground">#FFFFFFFF</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:isLightTheme">true</item>
        <item name="colorPrimary">#FF1A73E8</item>
        <item name="colorPrimaryDark">#FF0B57D0</item>
        <item name="colorAccent">#FF1A73E8</item>
        <item name="android:colorAccent">#FF1A73E8</item>
        <item name="mediaRouteTheme">@style/VesperPlayerMediaRouteTheme</item>
        <item name="mediaRouteButtonStyle">@style/Widget.MediaRouter.Light.MediaRouteButton</item>
    </style>

    <style name="VesperPlayerMediaRouteTheme" parent="Theme.MediaRouter.Light">
        <item name="android:windowBackground">#FFFFFFFF</item>
        <item name="android:colorBackground">#FFFFFFFF</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="colorBackgroundFloating">#FFFFFFFF</item>
    </style>
</resources>
