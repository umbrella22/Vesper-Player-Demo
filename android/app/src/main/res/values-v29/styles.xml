<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 10+ (API 29): explicitly disable the system-enforced contrast scrim
         that appears on the status bar when background is white/light.
         windowDrawsSystemBarBackgrounds + transparent colors allow Flutter's
         SystemUiOverlayStyle to fully control the bars. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>