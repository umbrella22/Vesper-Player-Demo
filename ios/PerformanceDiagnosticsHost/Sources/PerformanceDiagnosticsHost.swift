import Foundation

/// Build-only target that resolves the optional diagnostics SwiftPM product.
public enum PerformanceDiagnosticsHost {
    public static let artifactName = "VesperPlayerPerformanceDiagnosticsPlugin"
}
