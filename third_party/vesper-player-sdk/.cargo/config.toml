[env]
PKG_CONFIG = { value = "scripts/desktop/pkg-config-wrapper.sh", relative = true }
# Provide a stable SDK name for the macOS framework link to prevent rustc from invoking xcrun during the cargo check# phase and reporting Xcode's stderr noise as a warning.
SDKROOT = { value = "macosx", force = false }
